From: Rene Kita <mail@rkta.de>
Date: Tue Oct 31 10:43:55 CET 2023
Subject: refactor openUrl scheme handling

Instead of breaking in this really long switch statement, except a
little more code duplication to make the control flow more obvious.
---
 url.c |   11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

--- a/url.c
+++ b/url.c
@@ -1762,7 +1762,8 @@ openURL(char *url, ParsedURL *pu, Parsed
 	    uf.scheme = pu->scheme;
 	    return uf;
 	}
-	break;
+	uf.stream = newInputStream(sock);
+	return uf;
     case SCM_HTTP:
 #ifdef USE_SSL
     case SCM_HTTPS:
@@ -1891,7 +1892,8 @@ openURL(char *url, ParsedURL *pu, Parsed
 		request->enctype == FORM_ENCTYPE_MULTIPART)
 		write_from_file(sock, request->body);
 	}
-	break;
+	uf.stream = newInputStream(sock);
+	return uf;
 #ifdef USE_GOPHER
     case SCM_GOPHER:
 	p = pu->file;
@@ -1956,7 +1958,8 @@ openURL(char *url, ParsedURL *pu, Parsed
 	if(type != '\0') {
 	  pu->file = gophertmp->ptr;
 	}
-	break;
+	uf.stream = newInputStream(sock);
+	return uf;
 #endif				/* USE_GOPHER */
 #ifdef USE_NNTP
     case SCM_NNTP:
@@ -1993,8 +1996,6 @@ openURL(char *url, ParsedURL *pu, Parsed
     default:
 	return uf;
     }
-    uf.stream = newInputStream(sock);
-    return uf;
 }
 
 /* add index_file if exists */
