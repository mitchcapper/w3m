From: Rene Kita <mail@rkta.de>
Date: Sat Oct 28 11:31:40 CEST 2023
Subject: Get rid of MySignalHandler
---
 acinclude.m4 |   13 -------------
 config.h.in  |    3 ---
 configure    |   44 --------------------------------------------
 configure.ac |    1 -
 file.c       |   21 ++++++++++-----------
 frame.c      |    4 ++--
 ftp.c        |    5 ++---
 istream.c    |    2 +-
 main.c       |   24 ++++++++++--------------
 news.c       |    5 ++---
 proto.h      |    2 +-
 terms.c      |   26 ++++++++++++--------------
 url.c        |    7 +++----
 13 files changed, 43 insertions(+), 114 deletions(-)

--- a/file.c
+++ b/file.c
@@ -190,11 +190,10 @@ static struct compression_decoder {
 
 #define SAVE_BUF_SIZE 1536
 
-static MySignalHandler
+static void
 KeyAbort(SIGNAL_ARG)
 {
     LONGJMP(AbortLoading, 1);
-    SIGNAL_RETURN;
 }
 
 static void
@@ -1692,7 +1691,7 @@ loadGeneralFile(char *path, ParsedURL *v
     Buffer *volatile t_buf = NULL;
     int volatile searchHeader = SearchHeader;
     int volatile searchHeader_through = TRUE;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
     TextList *extra_header = newTextList();
     volatile Str uname = NULL;
     volatile Str pwd = NULL;
@@ -7271,7 +7270,7 @@ loadHTMLstream(URLFile *f, Buffer *newBu
 #ifdef USE_IMAGE
     int volatile image_flag;
 #endif
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
 #ifdef USE_M17N
     if (fmInitialized && graph_ok()) {
@@ -7436,7 +7435,7 @@ Buffer *
 loadHTMLString(Str page)
 {
     URLFile f;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
     Buffer *newBuf;
 
     init_stream(&f, SCM_LOCAL, newStrStream(page));
@@ -7487,7 +7486,7 @@ loadGopherDir0(URLFile *uf, ParsedURL *p
     Str lbuf, name, file, host, port, type;
     char *volatile p, *volatile q;
     int link, pre;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 #ifdef USE_M17N
     wc_ces doc_charset = DocumentCharset;
 #endif
@@ -7642,7 +7641,7 @@ loadGeminiBuffer(URLFile *uf, Buffer *vo
     int hseq, len, nlines, pre = 0;
     clen_t linelen = 0, trbyte = 0;
     Lineprop *propBuf = NULL;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
     if (SETJMP(AbortLoading) != 0) {
 	goto _end;
@@ -7775,7 +7774,7 @@ loadBuffer(URLFile *uf, Buffer *volatile
 #ifdef USE_ANSI_COLOR
     Linecolor *colorBuffer = NULL;
 #endif
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
     if (newBuf == NULL)
 	newBuf = newBuffer(INIT_BUFFER_WIDTH);
@@ -7864,7 +7863,7 @@ loadImageBuffer(URLFile *uf, Buffer *new
     Str tmp, tmpf;
     FILE *src = NULL;
     URLFile f;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
     struct stat st;
     ParsedURL *pu = newBuf ? &newBuf->currentURL : NULL;
 
@@ -8192,7 +8191,7 @@ getNextPage(Buffer *buf, int plen)
 #ifdef USE_ANSI_COLOR
     Linecolor *colorBuffer = NULL;
 #endif
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
     if (buf->pagerSource == NULL)
 	return NULL;
@@ -8301,7 +8300,7 @@ save2tmp(URLFile uf, const char *tmpf)
 {
     FILE *ff;
     clen_t linelen = 0, trbyte = 0;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
     static JMP_BUF env_bak;
     volatile int retval = 0;
     unsigned char *volatile buf = NULL;
--- a/frame.c
+++ b/frame.c
@@ -8,7 +8,7 @@
 static JMP_BUF AbortLoading;
 struct frameset *renderFrameSet = NULL;
 
-static MySignalHandler
+static void
 KeyAbort(SIGNAL_ARG)
 {
     LONGJMP(AbortLoading, 1);
@@ -405,7 +405,7 @@ createFrameFile(struct frameset *f, FILE
 #endif
     char *d_target, *p_target, *s_target, *t_target;
     ParsedURL *currentURL, base;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
     int flag;
 
     if (f == NULL)
--- a/ftp.c
+++ b/ftp.c
@@ -45,11 +45,10 @@ static struct _FTP current_ftp = {
 
 static JMP_BUF AbortLoading;
 
-static MySignalHandler
+static void
 KeyAbort(SIGNAL_ARG)
 {
     LONGJMP(AbortLoading, 1);
-    SIGNAL_RETURN;
 }
 
 static Str
@@ -483,7 +482,7 @@ loadFTPDir0(ParsedURL *pu)
     char *realpathname, *fn, *q;
     char **flist;
     int i, nfile, nfile_max;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 #ifdef USE_M17N
     wc_ces doc_charset = DocumentCharset;
 
--- a/istream.c
+++ b/istream.c
@@ -180,7 +180,7 @@ newEncodedStream(InputStream is, char en
 int
 ISclose(InputStream stream)
 {
-    MySignalHandler(*prevtrap) (SIGNAL_ARG);
+    void (*prevtrap) (SIGNAL_ARG);
     if (stream == NULL)
         return -1;
     if (stream->base.close != NULL) {
--- a/main.c
+++ b/main.c
@@ -67,17 +67,17 @@ static AlarmEvent DefaultAlarm = {
     0, AL_UNSET, FUNCNAME_nulcmd, NULL
 };
 static AlarmEvent *CurrentAlarm = &DefaultAlarm;
-static MySignalHandler SigAlarm(SIGNAL_ARG);
+static void SigAlarm(SIGNAL_ARG);
 #endif
 
 #ifdef SIGWINCH
 static int need_resize_screen = FALSE;
-static MySignalHandler resize_hook(SIGNAL_ARG);
+static void resize_hook(SIGNAL_ARG);
 static void resize_screen(void);
 #endif
 
 #ifdef SIGPIPE
-static MySignalHandler SigPipe(SIGNAL_ARG);
+static void SigPipe(SIGNAL_ARG);
 #endif
 
 #ifdef USE_MARK
@@ -1439,7 +1439,7 @@ cmp_anchor_hseq(const void *a, const voi
 static void
 do_dump(Buffer *buf)
 {
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
     prevtrap = mySignal(SIGINT, intTrap);
     if (SETJMP(IntReturn) != 0) {
@@ -1639,20 +1639,18 @@ repBuffer(Buffer *oldbuf, Buffer *buf)
 }
 
 
-MySignalHandler
+void
 intTrap(SIGNAL_ARG)
 {				/* Interrupt catcher */
     LONGJMP(IntReturn, 0);
-    SIGNAL_RETURN;
 }
 
 #ifdef SIGWINCH
-static MySignalHandler
+static void
 resize_hook(SIGNAL_ARG)
 {
     need_resize_screen = TRUE;
     mySignal(SIGWINCH, resize_hook);
-    SIGNAL_RETURN;
 }
 
 static void
@@ -1667,14 +1665,13 @@ resize_screen(void)
 #endif				/* SIGWINCH */
 
 #ifdef SIGPIPE
-static MySignalHandler
+static void
 SigPipe(SIGNAL_ARG)
 {
 #ifdef USE_MIGEMO
     init_migemo();
 #endif
     mySignal(SIGPIPE, SigPipe);
-    SIGNAL_RETURN;
 }
 #endif
 
@@ -1833,7 +1830,7 @@ clear_mark(Line *l)
 static int
 srchcore(const char *str, int (*func) (Buffer *, const char *))
 {
-    MySignalHandler(*prevtrap) (SIGNAL_ARG);
+    void (*prevtrap) (SIGNAL_ARG);
     volatile int i, result = SR_NOTFOUND;
 
     if (str != NULL && str != SearchString)
@@ -2235,7 +2232,7 @@ DEFUN(pipesh, PIPE_SHELL, "Execute shell
 DEFUN(readsh, READ_SHELL, "Execute shell command and display output")
 {
     Buffer *buf;
-    MySignalHandler(*prevtrap) (SIGNAL_ARG);
+    void (*prevtrap) (SIGNAL_ARG);
     char *cmd;
 
     CurrentKeyData = NULL;	/* not allowed in w3m-control: */
@@ -6250,7 +6247,7 @@ DEFUN(execCmd, COMMAND, "Invoke w3m func
 }
 
 #ifdef USE_ALARM
-static MySignalHandler
+static void
 SigAlarm(SIGNAL_ARG)
 {
     char *data;
@@ -6286,7 +6283,6 @@ SigAlarm(SIGNAL_ARG)
 	    alarm(CurrentAlarm->sec);
 	}
     }
-    SIGNAL_RETURN;
 }
 
 
--- a/news.c
+++ b/news.c
@@ -25,11 +25,10 @@ static News current_news = { NULL, 0, NU
 
 static JMP_BUF AbortLoading;
 
-static MySignalHandler
+static void
 KeyAbort(SIGNAL_ARG)
 {
     LONGJMP(AbortLoading, 1);
-    SIGNAL_RETURN;
 }
 
 static Str
@@ -324,7 +323,7 @@ loadNewsgroup0(ParsedURL *pu)
     char *volatile scheme, *volatile group, *volatile list;
     int status, i, first, last;
     volatile int flag = 0, start = 0, end = 0;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 #ifdef USE_M17N
     wc_ces doc_charset = DocumentCharset, mime_charset;
 
--- a/proto.h
+++ b/proto.h
@@ -9,7 +9,7 @@
 #define W3M_PROTO_H__
 extern void nulcmd(void);
 extern void pushEvent(int cmd, void *data);
-extern MySignalHandler intTrap(SIGNAL_ARG);
+extern void intTrap(SIGNAL_ARG);
 extern void pgFore(void);
 extern void pgBack(void);
 extern void hpgFore(void);
--- a/terms.c
+++ b/terms.c
@@ -259,7 +259,7 @@ check_cygwin_console(void)
 }
 #endif				/* __CYGWIN__ */
 
-MySignalHandler reset_exit(SIGNAL_ARG), reset_error_exit(SIGNAL_ARG), error_dump(SIGNAL_ARG);
+void reset_exit(SIGNAL_ARG), reset_error_exit(SIGNAL_ARG), error_dump(SIGNAL_ARG);
 
 #ifndef SIGIOT
 #define SIGIOT SIGABRT
@@ -549,9 +549,9 @@ put_image_kitty(char *url, int x, int y,
     int c, i, j, m, t, is_anim;
     struct stat st;
     pid_t pid;
-    MySignalHandler(*volatile previntr) (SIGNAL_ARG);
-    MySignalHandler(*volatile prevquit) (SIGNAL_ARG);
-    MySignalHandler(*volatile prevstop) (SIGNAL_ARG);
+    void (*volatile previntr) (SIGNAL_ARG);
+    void (*volatile prevquit) (SIGNAL_ARG);
+    void (*volatile prevstop) (SIGNAL_ARG);
 
     if (!url)
 	return;
@@ -767,9 +767,9 @@ put_image_sixel(char *url, int x, int y,
 {
     pid_t pid;
     int do_anim;
-    MySignalHandler(*volatile previntr) (SIGNAL_ARG);
-    MySignalHandler(*volatile prevquit) (SIGNAL_ARG);
-    MySignalHandler(*volatile prevstop) (SIGNAL_ARG);
+    void (*volatile previntr) (SIGNAL_ARG);
+    void (*volatile prevquit) (SIGNAL_ARG);
+    void (*volatile prevstop) (SIGNAL_ARG);
 
     MOVE(y,x);
     flush_tty();
@@ -1081,7 +1081,7 @@ reset_tty(void)
         close_tty();
 }
 
-static MySignalHandler
+static void
 reset_exit_with_value(SIGNAL_ARG, int rval)
 {
 #ifdef USE_MOUSE
@@ -1090,28 +1090,26 @@ reset_exit_with_value(SIGNAL_ARG, int rv
 #endif				/* USE_MOUSE */
     reset_tty();
     w3m_exit(rval);
-    SIGNAL_RETURN;
 }
 
-MySignalHandler
+void
 reset_error_exit(SIGNAL_ARG)
 {
   reset_exit_with_value(SIGNAL_ARGLIST, 1);
 }
 
-MySignalHandler
+void
 reset_exit(SIGNAL_ARG)
 {
   reset_exit_with_value(SIGNAL_ARGLIST, 0);
 }
 
-MySignalHandler
+void
 error_dump(SIGNAL_ARG)
 {
     mySignal(SIGIOT, SIG_DFL);
     reset_tty();
     abort();
-    SIGNAL_RETURN;
 }
 
 void
@@ -2335,7 +2333,7 @@ do_getch(void)
 	return sysm_getch();
 }
 
-MySignalHandler
+void
 sysmouse(SIGNAL_ARG)
 {
     struct mouse_info mi;
--- a/url.c
+++ b/url.c
@@ -246,11 +246,10 @@ DefaultFile(int scheme)
     return NULL;
 }
 
-static MySignalHandler
+static void
 KeyAbort(SIGNAL_ARG)
 {
     LONGJMP(AbortLoading, 1);
-    SIGNAL_RETURN;
 }
 
 #ifdef USE_SSL
@@ -553,7 +552,7 @@ openSocket(char *const hostname,
     int a1, a2, a3, a4;
     unsigned long adr;
 #endif				/* not INET6 */
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
     if (fmInitialized) {
 	message(Sprintf(_("Opening socket..."))->ptr, 0, 0);
@@ -2176,7 +2175,7 @@ check_no_proxy(char *domain)
 {
     TextListItem *tl;
     volatile int ret = 0;
-    MySignalHandler(*volatile prevtrap) (SIGNAL_ARG) = NULL;
+    void (*volatile prevtrap) (SIGNAL_ARG) = NULL;
 
     if (NO_proxy_domains == NULL || NO_proxy_domains->nitem == 0 ||
 	domain == NULL)
--- a/acinclude.m4
+++ b/acinclude.m4
@@ -911,16 +911,3 @@ AC_TRY_COMPILE(
 [have_sigsetjmp="yes"; AC_DEFINE(HAVE_SIGSETJMP)],
 [have_sigsetjmp="no"])
 AC_MSG_RESULT($have_sigsetjmp)])
-#
-# ----------------------------------------------------------------
-# AC_W3M_SIGNAL_RETURN
-# ----------------------------------------------------------------
-AC_DEFUN([AC_W3M_SIGNAL],
-[AC_TYPE_SIGNAL
- AC_SUBST(RETSIGTYPE)
- AC_SUBST(SIGNAL_RETURN)
- if test x"$ac_cv_type_signal" = xvoid; then
-  AC_DEFINE(SIGNAL_RETURN,return)
- else
-  AC_DEFINE(SIGNAL_RETURN,return 0)
- fi])
--- a/config.h.in
+++ b/config.h.in
@@ -164,11 +164,8 @@ typedef long clen_t;
 
 #undef HAVE_SIGSETJMP
 
-#define RETSIGTYPE @RETSIGTYPE@
-typedef RETSIGTYPE MySignalHandler;
 #define SIGNAL_ARG int _dummy	/* XXX */
 #define SIGNAL_ARGLIST 0	/* XXX */
-#define SIGNAL_RETURN @SIGNAL_RETURN@
 
 #ifdef HAVE_SIGSETJMP
 #ifdef __MINGW32_VERSION
--- a/configure.ac
+++ b/configure.ac
@@ -163,7 +163,6 @@ case "$host_os" in
     ;;
 esac
 AC_W3M_SIGSETJMP
-AC_W3M_SIGNAL
 
 AC_SUBST(HELP_DIR)
 HELP_DIR="$datadir/$PACKAGE"
--- a/configure
+++ b/configure
@@ -652,8 +652,6 @@ CURRENT_VERSION
 DOCDIRS
 RC_DIR
 HELP_DIR
-SIGNAL_RETURN
-RETSIGTYPE
 HAVE_SIGSETJMP
 USE_BINMODE_STREAM
 LIBGC
@@ -10849,48 +10847,6 @@ fi
 rm -f core conftest.err conftest.$ac_objext conftest.beam conftest.$ac_ext
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $have_sigsetjmp" >&5
 printf "%s\n" "$have_sigsetjmp" >&6; }
-{ printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking return type of signal handlers" >&5
-printf %s "checking return type of signal handlers... " >&6; }
-if test ${ac_cv_type_signal+y}
-then :
-  printf %s "(cached) " >&6
-else $as_nop
-  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-#include <sys/types.h>
-#include <signal.h>
-
-int
-main (void)
-{
-return *(signal (0, 0)) (0) == 1;
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_compile "$LINENO"
-then :
-  ac_cv_type_signal=int
-else $as_nop
-  ac_cv_type_signal=void
-fi
-rm -f core conftest.err conftest.$ac_objext conftest.beam conftest.$ac_ext
-fi
-{ printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_type_signal" >&5
-printf "%s\n" "$ac_cv_type_signal" >&6; }
-
-printf "%s\n" "#define RETSIGTYPE $ac_cv_type_signal" >>confdefs.h
-
-
-
-
- if test x"$ac_cv_type_signal" = xvoid; then
-  printf "%s\n" "#define SIGNAL_RETURN return" >>confdefs.h
-
- else
-  printf "%s\n" "#define SIGNAL_RETURN return 0" >>confdefs.h
-
- fi
 
 
 HELP_DIR="$datadir/$PACKAGE"
