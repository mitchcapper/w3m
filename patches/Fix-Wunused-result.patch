--- a/file.c
+++ b/file.c
@@ -8355,11 +8355,17 @@ _doFileCopy(char *tmpf, char *defstr, in
 	}
 	lock = tmpfname(TMPF_DFL, ".lock")->ptr;
 #if defined(HAVE_SYMLINK) && defined(HAVE_LSTAT)
-	symlink(p, lock);
+	if (symlink(p, lock) == -1) {
+	    disp_err_message("Can't create lock file.", FALSE);
+	    return -1;
+	}
 #else
 	f = fopen(lock, "w");
-	if (f)
-	    fclose(f);
+	if (!f) {
+	    disp_err_message("Can't create lock file.", FALSE);
+	    return -1;
+	}
+	fclose(f);
 #endif
 	flush_tty();
 	pid = fork();
@@ -8463,10 +8469,16 @@ doFileSave(URLFile uf, char *defstr)
 	 */
 	lock = tmpfname(TMPF_DFL, ".lock")->ptr;
 #if defined(HAVE_SYMLINK) && defined(HAVE_LSTAT)
-	symlink(p, lock);
+	if (symlink(p, lock) == -1) {
+	    disp_err_message("Can't create lock file.", FALSE);
+	    return -1;
+	}
 #else
 	f = fopen(lock, "w");
-	if (f)
+	if (!f) {
+	    disp_err_message("Can't create lock file.", FALSE);
+	    return -1;
+	}
 	    fclose(f);
 #endif
 	flush_tty();
--- a/buffer.c
+++ b/buffer.c
@@ -713,10 +713,13 @@ readBufferCache(Buffer *buf)
 	if (l->bpos == 0) {
 	    basel = l;
 	    l->lineBuf = NewAtom_N(char, l->size + 1);
-	    fread(l->lineBuf, 1, l->size, cache);
+	    if (fread(l->lineBuf, 1, l->size, cache) < l->size && ferror(cache))
+		return -1;
 	    l->lineBuf[l->size] = '\0';
 	    l->propBuf = NewAtom_N(Lineprop, l->size);
-	    fread(l->propBuf, sizeof(Lineprop), l->size, cache);
+	    if (fread(l->propBuf, sizeof(Lineprop), l->size, cache) < l->size
+		    && ferror(cache))
+		return -1;
 	}
 	else if (basel) {
 	    l->lineBuf = basel->lineBuf + l->bpos;
@@ -730,7 +733,9 @@ readBufferCache(Buffer *buf)
 	if (colorflag) {
 	    if (l->bpos == 0) {
 		l->colorBuf = NewAtom_N(Linecolor, l->size);
-		fread(l->colorBuf, sizeof(Linecolor), l->size, cache);
+		if (fread(l->colorBuf, sizeof(Linecolor), l->size, cache) < l->size
+			&& ferror(cache))
+		    return -1;
 	    }
 	    else
 		l->colorBuf = basel->colorBuf + l->bpos;
--- a/etc.c
+++ b/etc.c
@@ -1478,7 +1478,7 @@ myExec(char *command)
     exit(127);
 }
 
-void
+int
 mySystem(char *command, int background)
 {
 #ifndef __MINGW32_VERSION
@@ -1489,15 +1489,15 @@ mySystem(char *command, int background)
 	    setup_child(FALSE, 0, -1);
 	    myExec(command);
 	}
+	return 0;
 #else
 	Str cmd = Strnew_charp("start /f ");
 	Strcat_charp(cmd, command);
-	system(cmd->ptr);
+	return system(cmd->ptr);
 #endif
     }
-    else
 #endif /* __MINGW32_VERSION */
-	system(command);
+    return system(command);
 }
 
 Str
--- a/image.c
+++ b/image.c
@@ -548,7 +548,8 @@ loadImage(Buffer *buf, int flag)
 		unlink(cache->file);
 	    */
 #if defined(HAVE_SYMLINK) && defined(HAVE_LSTAT)
-	    symlink(cache->file, cache->touch);
+	    /* Ignore the return value as we are exiting with 0 anyways. */
+	    (void)!symlink(cache->file, cache->touch);
 #else
 	    {
 		FILE *f = fopen(cache->touch, "w");
--- a/proto.h
+++ b/proto.h
@@ -624,7 +624,7 @@ extern Str romanNumeral(int n);
 extern Str romanAlphabet(int n);
 extern void setup_child(int child, int i, int f);
 extern void myExec(char *command);
-extern void mySystem(char *command, int background);
+extern int mySystem(char *command, int background);
 extern Str myExtCommand(char *cmd, char *arg, int redirect);
 extern Str myEditor(char *cmd, char *file, int line);
 extern int is_localhost(const char *host);
--- a/terms.c
+++ b/terms.c
@@ -677,9 +677,10 @@ save_gif(const char *path, u_char *heade
     int	fd;
 
     if ((fd = open(path, O_WRONLY|O_CREAT, 0600)) >= 0) {
-	write(fd, header, header_size) ;
-	write(fd, body, body_size) ;
-	write(fd, "\x3b" , 1) ;
+	/* Suppress -Wunused-result */
+	(void)!write(fd, header, header_size) ;
+	(void)!write(fd, body, body_size) ;
+	(void)!write(fd, "\x3b" , 1) ;
 	close(fd) ;
     }
 }
