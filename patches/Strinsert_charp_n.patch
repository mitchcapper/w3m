From: Rene Kita <mail@rkta.de>
Date: Tue Sep 17 17:37:57 CEST 2024
Subject: Strinsert_charp_n
Patch: patches/Strinsert_charp_n.patch

---
 Str.c    |    7 +++++++
 Str.h    |    1 +
 linein.c |   13 ++-----------
 3 files changed, 10 insertions(+), 11 deletions(-)

--- a/linein.c
+++ b/linein.c
@@ -504,17 +504,8 @@ delC(void)
     for (int i = CPos; i < CLen; i++)
 	strProp[i] = strProp[i + delta];
 
-    if (!is_passwd) {
-	while (ynkBuf->length + delta > ynkBuf->area_size) {
-	    if (ynkBuf->area_size == STR_LEN_MAX + 1) return;
-		Strgrow(ynkBuf);
-	}
-	/* TODO(rkta): Better use Str function, does not exist yet */
-	memmove(ynkBuf->ptr + delta, ynkBuf->ptr, ynkBuf->length);
-	memcpy(ynkBuf->ptr, &strBuf->ptr[CPos], delta);
-	ynkBuf->length += delta;
-	ynkBuf->ptr[ynkBuf->length] = '\0';
-    }
+    if (!is_passwd)
+	Strinsert_charp_n(ynkBuf, 0, &strBuf->ptr[CPos], delta);
     Strdelete(strBuf, CPos, delta);
     CLen -= delta;
 }
--- a/Str.h
+++ b/Str.h
@@ -47,6 +47,7 @@ void Strcat_m_charp(Str, ...);
 Str Strsubstr(Str, int, int);
 void Strinsert_char(Str, int, char);
 void Strinsert_charp(Str, int, const char *);
+void Strinsert_charp_n(Str s, int pos, const char *p, int n);
 void Strdelete(Str, int, int);
 void Strtruncate(Str, int);
 void Strlower(Str);
--- a/Str.c
+++ b/Str.c
@@ -296,6 +296,13 @@ Strinsert_char(Str s, int pos, char c)
 }
 
 void
+Strinsert_charp_n(Str s, int pos, const char *p, int n)
+{
+    while (n--)
+	Strinsert_char(s, pos++, *(p++));
+}
+
+void
 Strinsert_charp(Str s, int pos, const char *p)
 {
     while (*p)
