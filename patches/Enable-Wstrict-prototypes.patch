---
 Makefile.in |    1 +
 1 file changed, 1 insertion(+)

--- a/Makefile.in
+++ b/Makefile.in
@@ -50,6 +50,7 @@ DEFAULTS += -Wnull-dereference
 DEFAULTS += -Wold-style-declaration
 DEFAULTS += -Wold-style-definition
 DEFAULTS += -Wredundant-decls
+DEFAULTS += -Wstrict-prototypes
 DEFAULTS += -Wstring-compare
 DEFAULTS += -Wtype-limits
 DEFAULTS += -Wuninitialized
