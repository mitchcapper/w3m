From: Rene Kita <mail@rkta.de>
Date: Sat Apr 20 06:55:48 CEST 2024
Subject: hist numbering
Patch: patches/hist-numbering.patch

---
 history.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

--- a/history.c
+++ b/history.c
@@ -13,7 +13,7 @@ historyBuffer(Hist *hist)
 
     Strcat_charp(src, _("<html>\n<head><title>History Page</title></head>\n"));
     Strcat_charp(src, _("<body>\n<h1>History Page</h1>\n<hr>\n"));
-    Strcat_charp(src, "<ol>\n");
+    Strcat_charp(src, "<ol start=0>\n");
     if (hist && hist->list) {
 	for (item = hist->list->last; item; item = item->prev) {
 	    q = html_quote(item->ptr);
