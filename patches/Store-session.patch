---
 config.h.in            |    1 
 doc-de/README.func     |    1 
 doc/README.func        |    1 
 doc/w3m.1              |   20 ++++++++
 fm.h                   |    2 
 main.c                 |  122 ++++++++++++++++++++++++++++++++++++++++++++++++-
 proto.h                |    1 
 rc.c                   |    4 +
 scripts/w3mhelp.cgi.in |    3 -
 9 files changed, 152 insertions(+), 3 deletions(-)

--- a/config.h.in
+++ b/config.h.in
@@ -22,6 +22,7 @@
 #define MOUSE_FILE   "mouse"
 #define COOKIE_FILE  "cookie"
 #define HISTORY_FILE "history"
+#define SESSION_FILE "session"
 
 #define PASSWD_FILE	RC_DIR "/passwd"
 #define PRE_FORM_FILE	RC_DIR "/pre_form"
--- a/main.c
+++ b/main.c
@@ -1,5 +1,6 @@
 /* vi: set sw=4 ts=8 ai sm noet : */
 #define MAINPROGRAM
+#include <errno.h>
 #include "fm.h"
 #include <stdio.h>
 #include <signal.h>
@@ -107,6 +108,9 @@ int prec_num = 0;
 int prev_key = -1;
 int on_target = 1;
 static int add_download_list = FALSE;
+static char *session_file = NULL;
+static int unlink_session;
+static void _strSession(char *sf);
 
 void set_buffer_environ(Buffer *);
 static void save_buffer_position(Buffer *buf);
@@ -215,6 +219,8 @@ fusage(FILE * f, int err)
 #endif
     fprintf(f, "    -B               load bookmark\n");
     fprintf(f, "    -bookmark file   specify bookmark file\n");
+    fprintf(f, "    -R               restore from session file\n");
+    fprintf(f, "    -session file    specify session file\n");
     fprintf(f, "    -T type          specify content-type\n");
     fprintf(f, "    -m               internet message mode\n");
     fprintf(f, "    -v               visual startup mode\n");
@@ -442,6 +448,7 @@ main(int argc, char **argv)
     char *default_type = NULL;
     char *post_file = NULL;
     Str err_msg;
+    int opt_restore = FALSE;
 #ifdef USE_M17N
     char *Locale = NULL;
     wc_uint8 auto_detect;
@@ -664,6 +671,13 @@ main(int argc, char **argv)
 		    BookmarkFile = cleanupName(tmp->ptr);
 		}
 	    }
+	    else if (!strcmp("-R", argv[i]))
+		opt_restore = TRUE;
+	    else if (!strcmp("-session", argv[i])) {
+		if (++i >= argc)
+		    usage();
+		session_file = argv[i];
+	    }
 	    else if (!strcmp("-F", argv[i]))
 		RenderFrame = TRUE;
 	    else if (!strcmp("-W", argv[i])) {
@@ -913,8 +927,46 @@ main(int argc, char **argv)
     if (ssl_known_hosts)
 	known_hosts = load_known_hosts();
 
-#ifdef USE_M17N
-#endif
+    /* Restore a previously saved session */
+    if (RestoreSession || opt_restore) {
+	FILE *fp;
+	Str line;
+	char *sf, **session;
+	int max = 16, n = 0;
+
+	sf = session_file ? session_file : rcFile(SESSION_FILE);
+	session = New_N(char *, max);
+	if (!(fp = fopen(sf, "r"))) {
+	    Str err = Sprintf("Cannot restore session %s - %s", sf,
+			      strerror(errno));
+	    disp_err_message(err->ptr, FALSE);
+	    fmTerm();
+	    return 1;
+	}
+
+	for (;;) {
+	    line = Strfgets(fp);
+	    Strchop(line);
+	    if (line->length == 0)
+		break;
+	    if (n > max) {
+		max <<= 1;
+		session = New_Reuse(char *, session, max);
+	    }
+	    session[n++] = line->ptr;
+	}
+
+	for (int i = 0; i < load_argc; i++) {
+	    if (n > max) {
+		max <<= 1;
+		New_Reuse(char *, session, max);
+	    }
+	    session[n++] = load_argv[i];
+	}
+	load_argv = session;
+	load_argc = n;
+	unlink_session = !session_file;
+    }
 
     if (w3m_backend)
 	backend();
@@ -2608,6 +2660,10 @@ _quitfm(int confirm)
 #endif				/* USE_HISTORY */
     if ((err = save_known_hosts()))
 	perror(_("Cannot save known hosts"));
+    if (unlink_session)
+	unlink(rcFile(SESSION_FILE));
+    if (StoreSession)
+	_strSession(NULL);
     if (deprecated)
 	fprintf(stderr, "%s\n%s\n%s\n",
 		"DEPRECATION WARNING",
@@ -4489,6 +4545,68 @@ DEFUN(adBmark, ADD_BOOKMARK, "Add curren
 		request);
 }
 
+void
+_strSession(char *sf)
+{
+    Buffer *buf;
+    FILE *f;
+    TabBuffer *tab;
+    ParsedURL *url;
+    char *sep;
+
+    if (!sf)
+	sf = session_file ? session_file : rcFile(SESSION_FILE);
+    if (!(f = fopen(sf, "w"))) goto fail;
+
+    sep = "";
+    for (tab = CurrentTab; tab->prevTab; tab = tab->prevTab);
+    for ( ; tab; tab = tab->nextTab) {
+	fputs(sep, f);
+	for (buf = tab->firstBuffer; buf; buf = buf->nextBuffer) {
+	    if ((url = baseURL(buf)))
+		fprintf(f, "%s\n", parsedURL2Str(url)->ptr);
+	    /*
+	     * Needs refactoring of HistoryPage handling first.
+	    else if (!strcmp(buf->buffername, "HistoryPage"))
+		fprintf(f, "%s\n", buf->buffername);
+	    */
+	    else
+		disp_err_message(buf->buffername, FALSE);
+	}
+	sep = "\n";
+    }
+
+    int r;
+    if ((r = fclose(f)) == EOF) goto fail;
+    unlink_session = 0;
+    return;
+
+fail:
+    disp_err_message(strerror(errno), FALSE);
+    return;
+}
+
+/* Store session */
+DEFUN(strSession, STRSESS, "Store session")
+{
+    char *def, *sf;
+
+    def = session_file ? session_file : rcFile(SESSION_FILE);
+    sf = inputFilenameHist(Strnew_m_charp("Session file [",
+					  def,
+					  "]? ", NULL)->ptr,
+			   NULL, LoadHist);
+    if (!*sf)
+	sf = def;
+    _strSession(sf);
+#ifdef USE_COOKIE
+    save_cookies();
+#endif
+#ifdef USE_HISTORY
+    saveUrlHistory();
+#endif
+}
+
 /* option setting */
 DEFUN(ldOpt, OPTIONS, "Display options setting panel")
 {
--- a/proto.h
+++ b/proto.h
@@ -87,6 +87,7 @@ extern void goMain(void);
 extern void gorURL(void);
 extern void ldBmark(void);
 extern void adBmark(void);
+extern void strSession(void);
 extern void ldOpt(void);
 extern void setOpt(void);
 extern void pginfo(void);
--- a/rc.c
+++ b/rc.c
@@ -73,6 +73,8 @@ static int OptionEncode = FALSE;
 #define CMT_HISTORY	 N_("Use URL history")
 #define CMT_HISTSIZE     N_("Number of remembered URL")
 #define CMT_SAVEHIST     N_("Save URL history")
+#define CMT_LOADSESS	 N_("Restore session on start")
+#define CMT_SAVESESS	 N_("Save session on quit")
 #define CMT_FRAME        N_("Render frames automatically")
 #define CMT_ARGV_IS_URL  N_("Treat argument without scheme as URL")
 #define CMT_TSELF        N_("Use _self as default target")
@@ -517,6 +519,8 @@ struct param_ptr params3[] = {
     {"history", P_INT, PI_TEXT, (void *)&URLHistSize, CMT_HISTSIZE, NULL},
     {"save_hist", P_INT, PI_ONOFF, (void *)&SaveURLHist, CMT_SAVEHIST, NULL},
 #endif				/* USE_HISTORY */
+    {"load_sess", P_INT, PI_ONOFF, (void *)&RestoreSession, CMT_LOADSESS, NULL},
+    {"save_sess", P_INT, PI_ONOFF, (void *)&StoreSession, CMT_SAVESESS, NULL},
     {"confirm_qq", P_INT, PI_ONOFF, (void *)&confirm_on_quit, CMT_CONFIRM_QQ,
      NULL},
     {"exit_on_last", P_INT, PI_ONOFF, (void *)&exit_on_last, CMT_EXIT_ON_LAST,
--- a/fm.h
+++ b/fm.h
@@ -1085,6 +1085,8 @@ global int UseHistory init(TRUE);
 global int URLHistSize init(100);
 global int SaveURLHist init(TRUE);
 #endif				/* USE_HISTORY */
+global int RestoreSession init(FALSE);
+global int StoreSession init(FALSE);
 global int multicolList init(FALSE);
 
 #ifdef USE_M17N
--- a/doc/w3m.1
+++ b/doc/w3m.1
@@ -62,6 +62,9 @@ use -o high-intensity=true instead
 \fB\-M\fP
 monochrome display
 .TP
+\fB\-R\fP
+restore session
+.TP
 \fB\-no-mouse\fP
 deactivate mouse support
 .TP
@@ -204,6 +207,9 @@ use \fIfile\fR instead of the default bo
 \fB\-config \fIfile\fR
 use \fIfile\fR instead of the default configuration file
 .TP
+\fB\-session \fIfile\fR
+use \fIfile\fR instead of the default session file
+.TP
 \fB\-o \fIoption=value\fR
 modify one configuration item with an explicitly given value;
 without \fIoption=value\fR, equivalent to \fB\-show-option\fR
@@ -213,6 +219,20 @@ use debug mode (only for debugging)
 .TP
 \fB\-reqlog\fP
 log headers of HTTP communication in file \f(CW~/.w3m/request.log\fP
+.SH Session management
+If \fB\-R\fP is given, w3m will restore a session from a session file.
+If no session file is provided with \fB\-session\fR the default session file
+is used. The default session file will be deleted after restoring a session
+when exiting w3m.
+
+Without a given session file, when manually storing a session, w3m will ask
+for a filename.
+
+If a session file is given, it will not be deleted and will be overwritten if
+storing a session again.
+
+Automatic session storing and restoring can be configured via the options.
+
 .SH EXAMPLES
 .SS Pager-like usage
 .TP
--- a/doc-de/README.func
+++ b/doc-de/README.func
@@ -134,6 +134,7 @@ SHIFT_LEFT	Anzeigebereich nach links ver
 SHIFT_RIGHT	Anzeigebereich nach rechts versetzen
 SOURCE		Wechsle zwischen HTML-Wiedergabe und -Verarbeitung
 STOP_IMAGE	Bilder nicht mehr laden und anzeigen
+STRSESS		Speichere Session
 SUBMIT		Formular übermitteln
 SUSPEND 	w3m zum Hintergrundprozess machen
 TAB_GOTO	Öffne das angegebene Dokument in einem neuen Reiter
--- a/doc/README.func
+++ b/doc/README.func
@@ -137,6 +137,7 @@ SHIFT_LEFT	Shift screen left
 SHIFT_RIGHT	Shift screen right
 SOURCE		Toggle between HTML shown or processed
 STOP_IMAGE	Stop loading and drawing of images
+STRSESS		Store session
 SUBMIT		Submit form
 SUSPEND 	Suspend w3m to background
 TAB_GOTO	Open specified document in a new tab
--- a/scripts/w3mhelp.cgi.in
+++ b/scripts/w3mhelp.cgi.in
@@ -171,7 +171,8 @@ print "<P><A HREF=\"$keymap\">$head</A>\
 	     split(" ", "_mark nextMk prevMk reMark"));
 
 &show_keymap('Miscellaneous',
-	     split(" ", "mainMn ldhelp ldOpt dispVer cooLst ldHist svHist ldDL
+	     split(" ", "mainMn ldhelp ldOpt dispVer cooLst ldHist svHist
+		strSession ldDL
 		docCSet defCSet msgs userMessage
 		msToggle wrapToggle execCmd setAlarm setOpt setEnv defKey
 		reinit execsh susp qquitfm quitfm"));
