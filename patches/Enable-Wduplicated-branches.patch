---
 Makefile.in            |    1 +
 libwc/Makefile.in      |    1 +
 w3mimg/Makefile.in     |    1 +
 w3mimg/fb/Makefile.in  |    1 +
 w3mimg/win/Makefile.in |    1 +
 w3mimg/x11/Makefile.in |    1 +
 6 files changed, 6 insertions(+)

--- a/Makefile.in
+++ b/Makefile.in
@@ -39,6 +39,7 @@ DEFAULTS = -Wall
 DEFAULTS += -D_FORTIFY_SOURCE=2
 DEFAULTS += -O3 -g3 -flto
 DEFAULTS += -Wcast-qual
+DEFAULTS += -Wduplicated-branches
 DEFAULTS += -Wno-missing-field-initializers
 DEFAULTS += -Wno-unused-result
 DEFAULTS += -Wnull-dereference
--- a/libwc/Makefile.in
+++ b/libwc/Makefile.in
@@ -10,6 +10,7 @@ DEFAULTS= -Wall
 DEFAULTS += -D_FORTIFY_SOURCE=2
 DEFAULTS += -O3 -g3 -flto
 DEFAULTS += -Wcast-qual
+DEFAULTS += -Wduplicated-branches
 DEFAULTS += -Wno-missing-field-initializers
 DEFAULTS += -Wno-unused-result
 DEFAULTS += -Wnull-dereference
--- a/w3mimg/Makefile.in
+++ b/w3mimg/Makefile.in
@@ -10,6 +10,7 @@ DEFAULTS= -Wall
 DEFAULTS += -D_FORTIFY_SOURCE=2
 DEFAULTS += -O3 -g3 -flto
 DEFAULTS += -Wcast-qual
+DEFAULTS += -Wduplicated-branches
 DEFAULTS += -Wno-missing-field-initializers
 DEFAULTS += -Wno-unused-result
 DEFAULTS += -Wnull-dereference
--- a/w3mimg/fb/Makefile.in
+++ b/w3mimg/fb/Makefile.in
@@ -11,6 +11,7 @@ DEFAULTS= -Wall
 DEFAULTS += -D_FORTIFY_SOURCE=2
 DEFAULTS += -O3 -g3 -flto
 DEFAULTS += -Wcast-qual
+DEFAULTS += -Wduplicated-branches
 DEFAULTS += -Wno-missing-field-initializers
 DEFAULTS += -Wno-unused-result
 DEFAULTS += -Wnull-dereference
--- a/w3mimg/win/Makefile.in
+++ b/w3mimg/win/Makefile.in
@@ -11,6 +11,7 @@ DEFAULTS= -Wall
 DEFAULTS += -D_FORTIFY_SOURCE=2
 DEFAULTS += -O3 -g3 -flto
 DEFAULTS += -Wcast-qual
+DEFAULTS += -Wduplicated-branches
 DEFAULTS += -Wno-missing-field-initializers
 DEFAULTS += -Wno-unused-result
 DEFAULTS += -Wnull-dereference
--- a/w3mimg/x11/Makefile.in
+++ b/w3mimg/x11/Makefile.in
@@ -11,6 +11,7 @@ DEFAULTS= -Wall
 DEFAULTS += -D_FORTIFY_SOURCE=2
 DEFAULTS += -O3 -g3 -flto
 DEFAULTS += -Wcast-qual
+DEFAULTS += -Wduplicated-branches
 DEFAULTS += -Wno-missing-field-initializers
 DEFAULTS += -Wno-unused-result
 DEFAULTS += -Wnull-dereference
